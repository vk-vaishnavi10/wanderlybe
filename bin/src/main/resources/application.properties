# 🌍 Application Info
spring.application.name=wanderly
server.port=8085

# 🐬 MySQL Configuration (Docker)
spring.datasource.url=jdbc:mysql://wanderly-mysql:3306/wanderly?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Vai@091005#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ⚙️ Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# ❌ Remove explicit dialect — Hibernate auto-detects it
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# 🧾 Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# 🌐 CORS (React app)
# Allow both local and Docker-based frontend URLs
app.cors.allowed-origins=http://localhost:5173,http://wanderly-frontend:5173
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
app.cors.allowed-headers=*
app.cors.allow-credentials=true

# 🔐 OTP mock mode (temporary)
otp.mode=mock
wanderly.api.key=${WANDERLY_API_KEY}

