# ===============================
# 🧱 Stage 1 — Clone + Build the Spring Boot JAR
# ===============================
FROM maven:3.9.5-eclipse-temurin-21 AS build
WORKDIR /app

# ✅ Install git
RUN apt-get update && apt-get install -y git

# ✅ Clone your backend repo
RUN git clone --branch main https://github.com/vk-vaishnavi10/wanderlybe.git .

# ✅ Force Maven to use Java 21
ENV JAVA_HOME=/usr/lib/jvm/java-21-openjdk
ENV PATH="$JAVA_HOME/bin:$PATH"

# ✅ Download all dependencies (to speed up caching)
RUN mvn dependency:go-offline -B

# ✅ Build the Spring Boot JAR (skip tests & enforce release 21)
RUN mvn clean package -DskipTests -Dmaven.compiler.release=21

# ===============================
# 🚀 Stage 2 — Run the Spring Boot app
# ===============================
FROM eclipse-temurin:21-jdk
WORKDIR /app

# ✅ Copy built JAR from previous stage
COPY --from=build /app/target/*.jar app.jar

# ✅ Create upload directory (for image uploads, profiles, etc.)
RUN mkdir -p /app/uploads/profile

# ✅ Mount uploads as volume (to persist data)
VOLUME ["/app/uploads"]

# ✅ Expose backend port
EXPOSE 8085

# ✅ Run Spring Boot JAR
ENTRYPOINT ["java", "-jar", "app.jar"]
