# 🌍 Application Info
spring.application.name=wanderly
server.port=8085

# 🐬 MySQL Configuration (Docker + Localhost + Minikube)
spring.datasource.url=jdbc:mysql://wanderly-mysql:3306/wanderly?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# 🏠 Localhost fallback (for local development)
spring.datasource.hikari.data-source-properties.serverTimezone=UTC

# 🌐 CORS — allow frontend (localhost + minikube IP)
wanderly.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173,http://localhost:30089
app.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173,http://localhost:30089


# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# 📁 File Upload Paths
file.upload-dir=uploads/profile/
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# 📌 Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# ✔ Backend service name for Nginx (Docker Compose)
wanderly.backend.url=http://wanderly-backend:8085
